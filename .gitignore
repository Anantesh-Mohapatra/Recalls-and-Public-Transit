# Quarto
/.quarto/
**/*.quarto_ipynb
/docs/
*_cache/
*_files/
/_freeze/

# LaTeX preview build artifacts (regenerated by latexmk)
output/tables/_preview.*

# renv: keep settings + lockfile, skip the (huge, machine-specific) library
renv/library/
renv/staging/
renv/python/
renv/local/
renv/cellar/

# R session detritus
.Rhistory
.RData
.Ruserdata

# OS
.DS_Store
Thumbs.db

# Secrets / env
.Renviron
.env

# Large raw data (exceeds GitHub's 100MB per-file limit; re-downloaded on demand)
data/raw/bls/la.data.64.County.txt

# Etc.
tmp/